syntax = "proto3";
package models.room;
import "models/doctor.proto";
import "models/patient.proto";

message Room {
  int32 roomNumber = 1;
  RoomStatus status = 2;
  models.patient.Patient patient = 3;
  models.doctor.Doctor doctor = 4;
}

enum RoomStatus {
  FREE = 0;
  OCCUPIED = 1;
}

